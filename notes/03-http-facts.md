# HTTP

- Uses TCP as the transport.
- TCP is connection oriented.

- port number is a unique identifier for each application on a server that is using HTTP
- http - "well known" (default) 80
- https - "well known" (default) 443